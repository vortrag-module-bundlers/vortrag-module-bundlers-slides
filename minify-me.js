const hello = (firstName, lastName) => {
  return `Hello ${firstName} ${lastName}`;
};

const helloWorld = (firstName, lastName) => {
  return `Hello world ${firstName} ${lastName}`;
};

const hello = (l, o) => `Hello ${l} ${o}`,
  helloWorld = (l, o) => `Hello world ${l} ${o}`;
